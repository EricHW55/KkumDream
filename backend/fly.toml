app = "kkumdream-backend"
primary_region = "nrt"

[env]
  ENVIRONMENT = "production"
  API_V1_PREFIX = "/api/v1"
  AI_MOCK_MODE = "true"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
